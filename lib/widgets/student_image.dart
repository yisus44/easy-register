import 'package:flutter/material.dart';

class StudentImage extends StatefulWidget {
  const StudentImage({Key? key}) : super(key: key);

  @override
  State<StudentImage> createState() => _StudentImageState();
}

class _StudentImageState extends State<StudentImage> {
  @override
  Widget build(BuildContext context) {
    return Container(
        
    );
  }
}
